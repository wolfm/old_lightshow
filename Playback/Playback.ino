//Wait for serial communication to begin

//When it begins

//Save the time as a start time

//Check that the protocol version matches

//Check that the number of LEDs matches what the Arduino is set up for

//While the next line isn't the EOF indicator
    //Set the timestamp to the time read in
    //Call set_pixel for every pixel in the array
    
    //While currentTime - startTime > timestamp
        //Wait
    
    //Call Show

//Loop back to beginning of program